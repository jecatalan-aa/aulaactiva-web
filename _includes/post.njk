---
layout: base.njk
---

{# Contenedor principal para centrar el contenido en una columna y dar espaciado #}
<div class="container mx-auto px-4 py-12 max-w-4xl">

  {# La clase 'prose' aplica automáticamente estilos hermosos al texto #}
  <article class="prose lg:prose-xl max-w-none">

    {# Título del post #}
    <h1 class="text-aa-darkblue">{{ title }}</h1>

    {# Metadatos: Fecha y Autor #}
    <p class="text-base text-gray-500">
      Publicado el: {{ date.toLocaleDateString('es-CL') }} por {{ author }}
    </p>

    {# Imagen principal, ahora estilizada y su tamaño controlado por la columna #}
    {% if thumbnail %}
      <img class="rounded-lg shadow-md mb-8" src="{{ thumbnail }}" alt="Imagen principal del post: {{ title }}">
    {% endif %}

    {# Aquí se inyecta el contenido del post (Markdown) ya formateado por la clase 'prose' #}
    {{ content | safe }}

  </article>

  <hr class="my-12">

  <a href="/blog/" class="text-aa-blue hover:underline font-semibold">← Volver al Blog</a>

</div>